# byobu-config
# sudo apt-get install python-newt

# disable window animations
gsettings set org.gnome.desktop.interface enable-animations false

# convert ppk
puttygen key.pem -o key.ppk -O private
puttygen key.pem -o key.pub -O public

# convert crt => pem
openssl x509 -in bundle.crt -out bundle.pem -outform PEM

# cert order
cat domain_com.crt \
COMODORSADomainValidationSecureServerCA.crt \
COMODORSAAddTrustCA.crt \
AddTrustExternalCARoot.crt > domain.crt

# caps => ctrl in terminal
# sudo vi /etc/default/keyboard
# XKBOPTIONS="ctrl:nocaps"
# sudo dpkg-reconfigure keyboard-configuration


# bt manual start
# sudo pactl load-module module-bluetooth-discover

# add missing firmware
# sudo apt-get install b43-fwcutter firmware-b43-installer


# bios
# apt-get install smbios-utils
# getSystemId
# biosfile.exe –writehdrfile
# modprobe dell_rbu
# dellBiosUpdate –u –f headerfile.hdr --reboot
# apt-get purge smbios-utils
